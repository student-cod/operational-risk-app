# operational-risk-app
A machine learning web app that predicts operational task delay risk using priority, duration, complexity, team load, and past delays. It combines rule-based logic with ML for better accuracy. Built with Streamlit, supports CSV uploads, real-time predictions, and is Dockerized for cloud deployment.
